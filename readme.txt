Finds all valid email addresses in a text file. Useful for both extracting and validating addresses, or both at once. 

to run: 
1. put the text you want to parse into "file_to_read.txt"
2. Run extract_email_addressses.py
3. email addresses will be printed to the terminal as output
